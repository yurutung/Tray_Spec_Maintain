# Tray_Spec_Maintain

1. install electron-forge
    ```powershell
    npm install -g electron-forge
    ```
2. install package
    ```powershell
    npm install
    ```
3. start
    ```powershell
    npm run start
    ```


* how to enum?
    tray_spec、tray_msl